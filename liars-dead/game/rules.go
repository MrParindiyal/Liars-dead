package game

func LegalActions(r *GameRoom, p *Player) []Action {
	return nil // TODO : implement game logic & rules
}

func ApplyAction(r *GameRoom, p *Player, a Action) error {
	return nil // TODO : apply rules to player actions
}